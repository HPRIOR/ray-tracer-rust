#![allow(dead_code, unused_imports)]

use module_lib::{
    exercises::{
        clock::clock::create_clock, projectile_test::projectile::create_projectile_canvas,
        ray_sphere::ray_sphere::render_sphere,
    },
    shapes::sphere::Sphere,
};

fn main() {
    // create_projectile_canvas("projectile");
    // create_clock();
    // render_sphere();
}
