#![allow(dead_code, unused_imports)]

use module_lib::exercises::{projectile_test::projectile::create_projectile_canvas, clock::clock::create_clock, ray_sphere::ray_sphere::render_sphere};

fn main() {
    //create_projectile_canvas("projectile");
    // create_clock();
    render_sphere();
}
